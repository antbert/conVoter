# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                                  controllers.Application.index()
GET         /login                             controllers.Application.index()
GET         /voting                             controllers.Application.index()
GET         /greeter                           controllers.Application.sockHandler()
POST        /login                             @controllers.Authorisation.login()
GET         /fillDatabase                      @controllers.Base.fillDatabase()
GET         /competition/*competitionId        @controllers.RestGet.competition(competitionId)

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                      controllers.Assets.at(path="/public", file)
